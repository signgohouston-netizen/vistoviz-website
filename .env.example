# VistoViz — environment variables (set the real values in Vercel → Settings → Environment Variables).
# This file documents the names only. NEVER commit real secrets/credentials.

# --- Lead capture (already used by /api/lead and /api/leads) ---
# MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/?retryWrites=true&w=majority
# ADMIN_KEY=your-admin-password-for-the-/admin.html-page

# --- ResellerClub storefront/API (future integration — not active yet) ---
# Placeholders only. Do not hard-code credentials in the client or commit real values.
# RESELLERCLUB_RESELLER_ID=
# RESELLERCLUB_API_KEY=
# RESELLERCLUB_API_BASE_URL=https://httpapi.com/api
